# PDFs

Run `python scripts/download_pdfs.py` from the repository root to download open-access PDFs listed in `data/papers.json`.

This folder intentionally starts empty. Do not commit copyrighted PDFs unless the license allows redistribution.
